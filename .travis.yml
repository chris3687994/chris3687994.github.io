language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "chris3687994"
  - git config user.email "newskyline@vip.qq.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${hexo_token}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - source
env:
 global:
   - GH_REF: github.com/chris3687994/chris3687994@github.oi.git